<div class="container margin-from-navbar dashboard-color">
<br>
<br>
<br>
  <div class="">
    <%# nav start %>
      <nav class="nav nav-tabs">
        <a class="nav-link active link-font" aria-current="page" href="#writings" data-bs-toggle="tab">📑</a>
        <a class="nav-link link-font" href="#newwriting" data-bs-toggle="tab">✍🏻</a>
        <a class="nav-link link-font" href="#profile" data-bs-toggle="tab">👱🏻‍♂️</a>
      </nav>
    <%# nav end %>

    <div class="container-60">
      <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade show active" id="writings" role="tabpanel">
          <ol class="ol-kill-padding">
            <% @writings.each do |writing| %>
              <%= link_to writing_path(writing) do %>
                <div class="card-product">
                  <%= cl_image_tag(writing.cover.key) %>
                  <div class="card-product-infos">
                    <div class="d-flex w-100 justify-content-between">
                      <h2><%= writing.title %></h2>
                      <div class="d-flex dasboard_card_icons">
                        <%= link_to  "🖋", edit_writing_path(writing) %>
                            <span>&nbsp;&nbsp;</span>
                        <%= link_to  "🗑", writing_path(writing),
                            data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
                      </div>
                    </div>
                    <span class="work-date"><%= writing.date.strftime("%d of %B, %Y") + " | " %></span>
                    <span class="work-text">text</span>
                    <p style="color: black !important;"><%= writing.tagline%></p>
                  </div>
                </div>
              <% end  %>
            <% end %>
          </ol>
        </div>

        <div class="tab-pane fade show" id="newwriting" role="tabpanel">
          <%= simple_form_for(@writing) do |f| %>
            <%= f.input :title, label: false, placeholder: "Title" %>
            <%#= f.input :author, label: false, placeholder: "Author" %>
            <%#= f.input :body %>
            <%= f.input :tagline, label: false, placeholder: "Tagline" %>
            <%= f.rich_text_area :rich_body %>
            <%= f.input :date, html5: true, label: false  %>
            <%= f.input :cover, as: :file, label: :photo %>
            <div class="btn w-100 dashboard-button">
              <%= f.submit %>
            </div>
            <br>
            <br>
            <br>
          <% end %>
        </div>

        <div class="tab-pane fade show" id="profile" role="tabpanel">

          <%= simple_form_for(@user, url: registration_path(@user), html: { method: :put }) do |f| %>
            <%= f.error_notification %>

            <div class="form-inputs">
              <%= f.input :avatar, as: :file, label: :avatar %>
              <%#= f.input :moodboard,
                          as: :file,
                          input_html: { multiple: true },
                          label: :moodboard %>
              <%= f.input :cv, as: :file, label: :cv %>
              <a id="cv-downloader" target="_blank" href="<%= cl_image_path @cv %>">Download my cv</a>
              <br>
              <br>
              <%= f.input :bio,
                          # label: false,
                          placeholder: "Bio",
                          required: false %>
              <%= f.input :contact_mail,
                          # label: false,
                          placeholder: "Contact mail",
                          hint: "This is the mail you want to show on the contact page",
                          required: false %>
              <%= f.input :quotes,
                          # label: false,
                          placeholder: "Quotes",
                          hint: "Say something fun for your footer!",
                          required: false %>
              <%= f.input :email, required: true, autofocus: true %>

              <%= f.input :password,
                          hint: "leave it blank if you don't want to change it",
                          required: false,
                          input_html: { autocomplete: "new-password" } %>
              <%= f.input :password_confirmation,
                          required: false,
                          input_html: { autocomplete: "new-password" } %>
              <%= f.input :current_password,
                          hint: "we need your current password to confirm your changes",
                          required: true,
                          input_html: { autocomplete: "current-password" } %>
            </div>

            <div class="form-actions">
              <%= f.button :submit, "Update", class: "w-100 dashboard-button", id: "text-white" %>
            </div>
            <br>
            <br>
            <br>
          <% end %>
        </div>

    </div>
  </div>
</div>

